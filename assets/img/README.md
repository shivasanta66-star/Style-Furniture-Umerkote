Shop photographs go here.

Point an `<image-slot>` in `index.html` at a file with `src="assets/img/…"`.
Until then each slot shows a caption describing the shot it needs.

Use real photos taken inside the shop, exported as WebP at about 1600px on the
long edge. The whole page should stay under 1.5 MB.
